
task wrapper(type: Wrapper) {
    // 声明gradle使用的版本
    gradleVersion = '2.1'
    // 默认的gradlew命令太长简化为'g'
    scriptFile = 'g'
}

task helloWorld << {
    println "Hello World!"
}

task copyFile(type: Copy) {
    from "xml"
    into "destination"
}
